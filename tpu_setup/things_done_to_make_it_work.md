1. Create everything inside ``aic`` directory
2. Created ``install_dependencies.sh`` file in ``aic/openpi``
3. Created ``docker_images_save_here.sh`` file to save docker iamges in persistent files as it takes time to create those
